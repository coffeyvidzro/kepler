name: Server CI

on:
  pull_request:
    paths:
      - ".github/workflows/server.yml"
      - "internal/database/queries/message_templates.sql"
      - "internal/database/queries/message_template_versions.sql"
      - "internal/modules/messagetemplate/repository.go"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: server-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Regenerate message template sqlc
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: fix/message-templates-sqlc-repository
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - name: Generate sqlc
        run: go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate

      - name: Format repository
        run: gofmt -w internal/modules/messagetemplate/repository.go

      - name: Test message template packages
        run: go test ./internal/modules/messagetemplate ./internal/transport/http/messagetemplate

      - name: Commit generated output
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add internal/database/sqlc internal/modules/messagetemplate/repository.go
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Regenerate message template sqlc"
          git push origin HEAD:fix/message-templates-sqlc-repository
